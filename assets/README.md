# assets

存放笔记配图：架构图、时序图、流程图、实验截图等。

约定：

- 文件名使用 `kebab-case`，并带上所属主题前缀，例如 `04-retrieval-hybrid-search-flow.png`；
- 优先使用可编辑源文件（`.drawio` / `.excalidraw` / mermaid 文本），同时提交导出的 `.png` 或 `.svg`；
- 单张图片建议小于 500 KB，大图先压缩再提交；
- 引用方式：`![描述](../assets/xxx.png)`。
